import type { CTA, NavItem, VariantProps } from "@/components/sections/types";
import { LogoBlock } from "@/components/sections/shared";

type NavbarProps = VariantProps & { logoText: string; logoImage?: string; items?: NavItem[]; cta?: CTA };
export default function Navbar(props: NavbarProps) { if (props.variant === "luxury") return <LuxuryNavbar {...props} />; if (props.variant === "bold") return <BoldNavbar {...props} />; return <BasicNavbar {...props} />; }
function LuxuryNavbar({ logoText, logoImage, items = [], cta }: NavbarProps) { return <nav className="sticky top-0 z-40 border-b border-[color:var(--color-primary)] bg-[var(--color-bg)] px-5 py-4 text-[var(--color-text)] backdrop-blur-xl md:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6"><div className="flex items-center gap-3"><LogoBlock image={logoImage} label={logoText} variant="luxury" /><span className="font-serif text-xl">{logoText}</span></div><div className="hidden items-center gap-7 text-sm opacity-70 md:flex">{items.map((item) => <a key={item.label} href={item.href} className="transition hover:text-[var(--color-accent)]">{item.label}</a>)}</div>{cta ? <a href={cta.href || "#contact"} className="rounded-full border border-[color:var(--color-primary)] px-4 py-2 text-xs font-bold uppercase tracking-[0.16em] text-[var(--color-accent)]">{cta.label}</a> : null}</div></nav>; }
function BasicNavbar({ logoText, logoImage, items = [], cta }: NavbarProps) { return <nav className="sticky top-0 z-40 border-b border-[color:var(--color-primary)] bg-[var(--color-bg)] px-5 py-4 text-[var(--color-text)] backdrop-blur md:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6"><div className="flex items-center gap-3"><LogoBlock image={logoImage} label={logoText} variant="basic" /><span className="font-semibold">{logoText}</span></div><div className="hidden items-center gap-6 text-sm opacity-70 md:flex">{items.map((item) => <a key={item.label} href={item.href} className="hover:text-[var(--color-primary)]">{item.label}</a>)}</div>{cta ? <a href={cta.href || "#contact"} className="rounded-xl bg-[var(--color-primary)] px-4 py-2 text-sm font-semibold text-[var(--color-bg)]">{cta.label}</a> : null}</div></nav>; }
function BoldNavbar({ logoText, logoImage, items = [], cta }: NavbarProps) { return <nav className="sticky top-0 z-40 border-b-4 border-[color:var(--color-text)] bg-[var(--color-bg)] px-5 py-4 text-[var(--color-text)] md:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6"><div className="flex items-center gap-3"><LogoBlock image={logoImage} label={logoText} variant="bold" /><span className="text-xl font-black uppercase">{logoText}</span></div><div className="hidden items-center gap-5 text-sm font-black uppercase md:flex">{items.map((item) => <a key={item.label} href={item.href} className="border-b-4 border-transparent hover:border-[color:var(--color-primary)]">{item.label}</a>)}</div>{cta ? <a href={cta.href || "#contact"} className="border-4 border-[color:var(--color-text)] bg-[var(--color-text)] px-4 py-2 text-sm font-black uppercase text-[var(--color-bg)] shadow-[4px_4px_0_var(--color-primary)]">{cta.label}</a> : null}</div></nav>; }
