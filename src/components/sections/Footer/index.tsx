import type { NavItem, VariantProps } from "@/components/sections/types";

type FooterProps = VariantProps & { logoText: string; tagline?: string; items?: NavItem[] };
export default function Footer(props: FooterProps) { if (props.variant === "luxury") return <LuxuryFooter {...props} />; if (props.variant === "bold") return <BoldFooter {...props} />; return <BasicFooter {...props} />; }
function LuxuryFooter({ logoText, tagline, items = [] }: FooterProps) { return <footer className="border-t border-[color:var(--color-primary)] bg-[var(--color-bg)] px-5 py-10 text-[var(--color-text)] md:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-6 md:flex-row md:items-center"><div><p className="font-serif text-2xl">{logoText}</p>{tagline ? <p className="mt-2 text-sm opacity-55">{tagline}</p> : null}</div><div className="flex flex-wrap gap-5 text-sm opacity-60">{items.map((item) => <a key={item.label} href={item.href}>{item.label}</a>)}</div></div></footer>; }
function BasicFooter({ logoText, tagline, items = [] }: FooterProps) { return <footer className="border-t border-[color:var(--color-primary)] bg-[var(--color-bg)] px-5 py-8 text-[var(--color-text)] md:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 md:flex-row md:items-center"><p className="font-semibold">{logoText}</p>{tagline ? <p className="text-sm opacity-70">{tagline}</p> : null}<div className="flex flex-wrap gap-4 text-sm opacity-70">{items.map((item) => <a key={item.label} href={item.href}>{item.label}</a>)}</div></div></footer>; }
function BoldFooter({ logoText, tagline, items = [] }: FooterProps) { return <footer className="border-t-4 border-[color:var(--color-text)] bg-[var(--color-bg)] px-5 py-8 text-[var(--color-text)] md:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 md:flex-row md:items-center"><p className="text-2xl font-black uppercase">{logoText}</p>{tagline ? <p className="font-black uppercase">{tagline}</p> : null}<div className="flex flex-wrap gap-4 text-sm font-black uppercase">{items.map((item) => <a key={item.label} href={item.href}>{item.label}</a>)}</div></div></footer>; }
