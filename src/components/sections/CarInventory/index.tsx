import type { VariantProps, VehicleItem } from "@/components/sections/types";
import { imageBackground, themeGradient } from "@/components/sections/shared";

type CarInventoryProps = VariantProps & { title: string; subtitle?: string; vehicles: VehicleItem[]; categories?: string[] };
export default function CarInventory(props: CarInventoryProps) { if (props.variant === "luxury") return <LuxuryInventory {...props} />; if (props.variant === "bold") return <BoldInventory {...props} />; return <BasicInventory {...props} />; }
function LuxuryInventory({ title, subtitle, vehicles, categories = [] }: CarInventoryProps) { return <section id="inventory" className="bg-[var(--color-bg)] px-5 py-20 text-[var(--color-text)] md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-6 border-b border-[color:var(--color-primary)] pb-8 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-[var(--color-accent)]">Car Rental / Sales</p><h2 className="mt-4 max-w-4xl font-serif text-4xl leading-none md:text-6xl">{title}</h2>{subtitle ? <p className="mt-5 max-w-2xl opacity-65">{subtitle}</p> : null}</div><div className="flex flex-wrap gap-2">{categories.map((category) => <span key={category} className="rounded-full border border-[color:var(--color-primary)] px-4 py-2 text-xs font-semibold uppercase tracking-[0.16em] text-[var(--color-accent)]">{category}</span>)}</div></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{vehicles.map((vehicle, index) => <article key={vehicle.name + index} className="overflow-hidden rounded-[1.75rem] border border-[color:var(--color-primary)] bg-[var(--color-secondary)] shadow-2xl transition hover:-translate-y-1"><div className="h-64 bg-cover bg-center" style={imageBackground(vehicle.image, themeGradient())} /><div className="p-6"><div className="flex items-start justify-between gap-4"><h3 className="text-2xl font-semibold">{vehicle.name}</h3>{vehicle.price ? <span className="rounded-full bg-[var(--color-primary)] px-3 py-1 text-sm font-semibold text-[var(--color-bg)]">{vehicle.price}</span> : null}</div><p className="mt-4 text-sm leading-6 opacity-65">{vehicle.description}</p><div className="mt-5 flex flex-wrap gap-2">{vehicle.specs?.map((spec) => <span key={spec} className="rounded-full border border-[color:var(--color-primary)] px-3 py-1 text-xs opacity-70">{spec}</span>)}</div></div></article>)}</div></div></section>; }
function BasicInventory({ title, subtitle, vehicles, categories = [] }: CarInventoryProps) { return <section id="inventory" className="bg-[var(--color-secondary)] px-5 py-16 text-[var(--color-text)] md:px-8"><div className="mx-auto max-w-7xl"><h2 className="text-3xl font-semibold md:text-5xl">{title}</h2>{subtitle ? <p className="mt-4 max-w-2xl opacity-70">{subtitle}</p> : null}<div className="mt-6 flex flex-wrap gap-2">{categories.map((category) => <span key={category} className="rounded-full bg-[var(--color-bg)] px-4 py-2 text-sm font-medium opacity-80 shadow-sm">{category}</span>)}</div><div className="mt-8 grid gap-4 md:grid-cols-3">{vehicles.map((vehicle, index) => <article key={vehicle.name + index} className="rounded-2xl border border-[color:var(--color-primary)] bg-[var(--color-bg)] p-4"><div className="h-48 rounded-xl bg-cover bg-center" style={imageBackground(vehicle.image, themeGradient())} /><h3 className="mt-5 text-xl font-semibold">{vehicle.name}</h3>{vehicle.price ? <p className="mt-1 font-semibold text-[var(--color-primary)]">{vehicle.price}</p> : null}<p className="mt-3 text-sm leading-6 opacity-70">{vehicle.description}</p></article>)}</div></div></section>; }
function BoldInventory({ title, subtitle, vehicles, categories = [] }: CarInventoryProps) { return <section id="inventory" className="bg-[var(--color-bg)] px-5 py-16 text-[var(--color-text)] md:px-8 md:py-24"><div className="mx-auto max-w-7xl"><h2 className="max-w-4xl text-5xl font-black uppercase leading-none md:text-7xl">{title}</h2>{subtitle ? <p className="mt-5 max-w-2xl border-4 border-[color:var(--color-text)] bg-[var(--color-secondary)] p-4 font-black shadow-[7px_7px_0_var(--color-primary)]">{subtitle}</p> : null}<div className="mt-7 flex flex-wrap gap-3">{categories.map((category) => <span key={category} className="border-4 border-[color:var(--color-text)] bg-[var(--color-text)] px-4 py-2 text-sm font-black uppercase text-[var(--color-bg)]">{category}</span>)}</div><div className="mt-10 grid gap-5 lg:grid-cols-3">{vehicles.map((vehicle, index) => <article key={vehicle.name + index} className="border-4 border-[color:var(--color-text)] bg-[var(--color-secondary)] shadow-[10px_10px_0_var(--color-primary)]"><div className="h-60 bg-cover bg-center" style={imageBackground(vehicle.image, themeGradient())} /><div className="p-5"><p className="text-sm font-black uppercase text-[var(--color-primary)]">{vehicle.category || "Vehicle"} / 0{index + 1}</p><h3 className="mt-2 text-3xl font-black uppercase leading-none">{vehicle.name}</h3>{vehicle.price ? <p className="mt-4 inline-block bg-[var(--color-text)] px-3 py-2 font-black uppercase text-[var(--color-bg)]">{vehicle.price}</p> : null}<p className="mt-4 font-bold leading-6 opacity-80">{vehicle.description}</p></div></article>)}</div></div></section>; }
