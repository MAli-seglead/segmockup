import type { VariantProps } from "@/components/sections/types";
import { imageBackground, themeGradient } from "@/components/sections/shared";

type GalleryProps = VariantProps & { title: string; subtitle?: string; images: string[] };
export default function Gallery(props: GalleryProps) { if (props.variant === "luxury") return <LuxuryGallery {...props} />; if (props.variant === "bold") return <BoldGallery {...props} />; return <BasicGallery {...props} />; }
function LuxuryGallery({ title, subtitle, images }: GalleryProps) { return <section id="gallery" className="bg-[var(--color-secondary)] px-5 py-20 text-[var(--color-text)] md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><h2 className="max-w-3xl font-serif text-4xl leading-none md:text-6xl">{title}</h2>{subtitle ? <p className="mt-4 max-w-xl opacity-70">{subtitle}</p> : null}<div className="mt-10 grid auto-rows-[180px] gap-4 md:grid-cols-4">{images.slice(0, 5).map((image, index) => <div key={image + index} className={(index === 0 ? "md:col-span-2 md:row-span-2 " : index === 3 ? "md:col-span-2 " : "") + "rounded-[1.5rem] bg-cover bg-center shadow-xl"} style={imageBackground(image, themeGradient())} />)}</div></div></section>; }
function BasicGallery({ title, subtitle, images }: GalleryProps) { return <section id="gallery" className="bg-[var(--color-secondary)] px-5 py-16 text-[var(--color-text)] md:px-8"><div className="mx-auto max-w-7xl"><h2 className="text-3xl font-semibold md:text-5xl">{title}</h2>{subtitle ? <p className="mt-4 opacity-70">{subtitle}</p> : null}<div className="mt-8 grid gap-4 sm:grid-cols-2 md:grid-cols-3">{images.slice(0, 6).map((image, index) => <div key={image + index} className="aspect-[4/3] rounded-2xl bg-cover bg-center" style={imageBackground(image, themeGradient())} />)}</div></div></section>; }
function BoldGallery({ title, subtitle, images }: GalleryProps) { return <section id="gallery" className="bg-[var(--color-text)] px-5 py-16 text-[var(--color-bg)] md:px-8 md:py-24"><div className="mx-auto max-w-7xl"><h2 className="text-5xl font-black uppercase leading-none md:text-7xl">{title}</h2>{subtitle ? <p className="mt-5 inline-block bg-[var(--color-primary)] p-4 font-black uppercase text-[var(--color-bg)]">{subtitle}</p> : null}<div className="mt-10 grid auto-rows-[180px] gap-4 md:grid-cols-4">{images.slice(0, 5).map((image, index) => <div key={image + index} className={(index === 1 ? "md:col-span-2 md:row-span-2 rotate-1 " : "") + "border-4 border-[color:var(--color-bg)] bg-cover bg-center shadow-[8px_8px_0_var(--color-primary)]"} style={imageBackground(image, themeGradient())} />)}</div></div></section>; }
